# tolossy core

Native rust app. Produces executable with bundled web app embedded into it.

## Build

```bash
cargo build --release
```

## Dev mode

```bash
DEBUG=true cargo run

# DEBUG version does not open browser tab so you can use vite app in dev mode
```
